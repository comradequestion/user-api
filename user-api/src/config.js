const config = {
    dev: {
        database: {
            host: '127.0.0.1',
            port: '27017',
            db_name: 'dev_db'
        },
        user_collection: 'users',
    }
};

export default config;
